import 'package:flutter/material.dart';

class RecentGymsCard extends StatelessWidget {
  const RecentGymsCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}