import 'package:flutter/material.dart';

class GymStatusBadge extends StatelessWidget {
  const GymStatusBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}